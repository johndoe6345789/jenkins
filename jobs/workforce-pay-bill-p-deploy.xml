<?xml version='1.1' encoding='UTF-8'?>
<flow-definition plugin="workflow-job">
  <actions/>
  <description>Deploy pipeline for workforce-pay-bill-p. Builds the nginx/SPA image from the repo's docker-compose.yml and brings the stack up on port 8893. Auto-triggered after a successful workforce-pay-bill-p build.</description>
  <keepDependencies>false</keepDependencies>
  <properties/>
  <definition class="org.jenkinsci.plugins.workflow.cps.CpsFlowDefinition" plugin="workflow-cps">
    <script>pipeline {
  agent any

  options {
    timestamps()
    buildDiscarder(logRotator(numToKeepStr: '15'))
    timeout(time: 30, unit: 'MINUTES')
    disableConcurrentBuilds()
  }

  stages {
    stage('Checkout') {
      steps {
        git branch: 'main', url: 'https://github.com/johndoe6345789/workforce-pay-bill-p.git'
      }
    }

    stage('Docker Preflight') {
      steps {
        sh '''
          docker version
          docker compose version
        '''
      }
    }

    stage('Build + Deploy') {
      steps {
        sh '''
          set -e
          docker compose up -d --build --remove-orphans
        '''
      }
    }

    stage('Status') {
      steps {
        sh 'docker compose ps'
      }
    }
  }

  post {
    success {
      echo 'workforce-pay-bill-p deployed - http://wardcrew.com:8893'
    }
    always {
      cleanWs()
    }
  }
}</script>
    <sandbox>true</sandbox>
  </definition>
  <triggers>
    <jenkins.triggers.ReverseBuildTrigger>
      <spec></spec>
      <upstreamProjects>workforce-pay-bill-p</upstreamProjects>
      <threshold>
        <name>SUCCESS</name>
        <ordinal>0</ordinal>
        <color>BLUE</color>
        <completeBuild>true</completeBuild>
      </threshold>
    </jenkins.triggers.ReverseBuildTrigger>
  </triggers>
  <disabled>false</disabled>
</flow-definition>
